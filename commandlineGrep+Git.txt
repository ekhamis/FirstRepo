grep
grep -w word
grep -wn word line number
grep -v exlude 

2> error.log


add		commit		push
INDEX		HEAD		

GIT COMMANDS:
global information tracker
git config
git remmote add origin <name>
git push -u origin
git init (make a directory localy)
git add (add file to tracker)
git add file-* (add all files)
git rm (remove the file from index and file system)
git rm --cached (remove only from the index)
git commit -m
git commit -am (all)
git commit --amend
git push
git log
git status

output: creat mode(file name) 

GIT BRANCHES:
